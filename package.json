{
  "name": "school-transport-ecosystem",
  "version": "1.0.0",
  "description": "Safaricom-linked School Transport Tracking and Security Platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "sync-assets": "node scripts/sync-assets.js",
    "dev:dashboard": "npm run sync-assets && npm run dev --workspace=apps/admin_dashboard",
    "build:dashboard": "npm run sync-assets && npm run build --workspace=apps/admin_dashboard"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2",
    "supabase": "^2.106.0"
  }
}

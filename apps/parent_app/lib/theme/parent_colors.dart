import 'package:flutter/material.dart';

/// Parent-app tokens aligned with [context/ui-context.md] (marketing + parent light).
class ParentColors {
  static const background = Color(0xFFF8F9FF);
  static const surface = Color(0xFFFFFFFF);
  static const ink = Color(0xFF0B1C30);
  static const muted = Color(0xFF3D4A3E);
  static const primary = Color(0xFF10B981);
  static const primaryFill = Color(0xFF047857);
  static const primarySoft = Color(0xFFE8F8F0);
  static const error = Color(0xFFEF4444);
  static const errorSoft = Color(0xFFFEE2E2);
}

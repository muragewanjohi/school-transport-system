import 'package:flutter/material.dart';

/// OnTheBus Driver light theme palette.
class AppColors {
  AppColors._();

  static const Color pageBg = Color(0xFFF8F9FF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF1F5F9);
  static const Color border = Color(0xFFE2E8F0);
  static const Color ink = Color(0xFF0B1C30);
  static const Color muted = Color(0xFF64748B);
  static const Color mutedLight = Color(0xFF94A3B8);

  /// Brand / AppBar green (prefer for chrome).
  static const Color primaryGreen = Color(0xFF006B32);

  /// Action / CTA green (existing heavy usage in buttons).
  static const Color actionGreen = Color(0xFF10B981);
}
